<odoo>
    <data noupdate="0">
        <!-- Creating Security Groups -->
        <record id="group_parent" model="res.groups">
            <field name="name">Parent</field>
            <field name="category_id" ref="module_category_name"/>
        </record>

        <record id="group_driver" model="res.groups">
            <field name="name">Driver</field>
            <field name="category_id" ref="module_category_name"/>
        </record>

        <record id="group_admin" model="res.groups">
            <field name="name">Administrator</field>
            <field name="category_id" ref="module_category_name"/>
        </record>

        <!-- Defining Access Control Rules -->
        <record id="model_ksba_school_parent_rule" model="ir.model.access">
            <field name="name">ksba.school access for parents</field>
            <field name="model_id" ref="model_ksba_school"/>
            <field name="group_id" ref="group_parent"/>
            <field name="perm_read" eval="1"/>
            <field name="perm_create" eval="0"/>
            <field name="perm_write" eval="0"/>
            <field name="perm_unlink" eval="0"/>
        </record>

        <!-- Repeat above record for each model and user role, adjusting permissions accordingly -->
    </data>
</odoo>